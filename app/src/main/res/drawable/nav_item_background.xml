<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Nền khi mục được chọn -->
    <item android:drawable="@drawable/rounded_green_background" android:state_checked="true" />
    <!-- Nền khi mục không được chọn -->
    <item android:drawable="@android:color/transparent" />
</selector>
